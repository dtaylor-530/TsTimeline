<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                       xmlns:timelineControl="clr-namespace:TsTimeline">

    <Style x:Key="HoldClipStyle" TargetType="{x:Type timelineControl:ClipBase}" BasedOn="{StaticResource {x:Type timelineControl:ClipBase}}">
        <Style.Resources>
            <Style TargetType="Thumb">
                <Setter Property="BorderBrush" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Height"
                        Value="{Binding ActualHeight , RelativeSource={RelativeSource AncestorType={x:Type Canvas}}}" />
                <Setter Property="Width" Value="5" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate>
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    Width="{TemplateBinding Width}"
                                    Height="{TemplateBinding Height}"
                                    CornerRadius="2" />
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Style.Resources>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate>
                    <Canvas x:Name="PART_CANVAS"
                            Width="{TemplateBinding Width}"
                            Height="{TemplateBinding Height}"
                            ClipToBounds="{TemplateBinding ClipToBounds}">
                        <Thumb x:Name="PART_CENTER" Background="Orange" Opacity="0.25"         Cursor="Hand"/>
                        <Thumb x:Name="PART_LEFT" Background="Orange" Opacity="0.55"        Cursor="Hand" />
                        <Thumb x:Name="PART_RIGHT" Background="Orange" Opacity="0.55"         Cursor="Hand"/>
                    </Canvas>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
</ResourceDictionary>