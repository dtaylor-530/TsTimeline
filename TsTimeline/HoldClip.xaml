<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                       xmlns:timelineControl="clr-namespace:TsTimeline">

    <Style x:Key="HoldClipStyle" TargetType="{x:Type timelineControl:ClipBase}" BasedOn="{StaticResource {x:Type timelineControl:ClipBase}}">
        <Style.Resources>
            <Style TargetType="Thumb">
                <Setter Property="BorderBrush" Value="Transparent" />
                <Setter Property="Background" Value="{Binding Background, RelativeSource={RelativeSource AncestorType=timelineControl:ClipBase}}" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Height"
                        Value="{Binding ActualHeight , RelativeSource={RelativeSource AncestorType={x:Type Canvas}}}" />
              
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate>
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    Width="{TemplateBinding Width}"
                                    Height="{TemplateBinding Height}"
                                    CornerRadius="2" />
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Style.Resources>
        <Setter Property="Background" Value="Orange"></Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate>

                    <Grid x:Name="PART_GRID" HorizontalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="5"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="5"/>
                        </Grid.ColumnDefinitions>

                        <Thumb x:Name="PART_LEFT"
                               Grid.Column="0"
                               Opacity="0.55"
                               Cursor="SizeWE"/>
                        <Thumb x:Name="PART_CENTER"
                               Grid.Column="1"
                               Opacity="0.25"
                               Cursor="Hand"/>

                        <Thumb x:Name="PART_RIGHT"
                               Grid.Column="2"
                               Opacity="0.55"
                               Cursor="SizeWE"/>
                    </Grid>

                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
</ResourceDictionary>