<UserControl x:Class="Demos.ProgressUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Demos"
             mc:Ignorable="d" 
                   d:Background="White"
             d:DataContext="{d:DesignInstance local:ProgressViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <TextBlock Text="Progress"   VerticalAlignment="Bottom"        Margin="4"></TextBlock>
        <Border BorderBrush="Gray"
                Margin="4"
                VerticalAlignment="Top"
                Grid.Row="1"
            BorderThickness="1"
            HorizontalAlignment="Center"
            CornerRadius="4"
          
            Height="20">
        <TextBlock Width="100" 
                    Margin="4"
                    FontWeight="Bold"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"
                   Text="{Binding Value, StringFormat=F1, RelativeSource={RelativeSource AncestorType=UserControl}}"></TextBlock>
    </Border>
    </Grid>
</UserControl>
